# 项目代理协作入口

本文档是项目级最高优先级协作入口，只保留必须遵守的总规则。详细红测、修复、review 和契约记录细则见 `docs/CONTRACT_WORKFLOW.md`。

当前默认由 Codex 承担规划者职责，负责维护总体规则、阶段计划和开发节奏；当老爷明确要求写用例、实现或 review 时，再切换到对应角色。

## 必读文档

- `docs/CORE_REQUIREMENTS.md`：产品主线、核心业务对象和业务边界。
- `docs/DEVELOPMENT_PLAN.md`：阶段拆分、交付顺序和当前开发重点。
- `docs/CONTRACT_WORKFLOW.md`：契约红测、业务修复、代码审核和契约记录细则。
- `docs/TESTING.md`：现有测试入口和人工验收说明。

## 项目主线

项目主线以 `docs/CORE_REQUIREMENTS.md` 为准。所有阶段设计、红测、实现和 review 都应围绕核心需求收束。

发现偏离主线、过早扩展非核心功能、绕过契约流程或把实现便利写成业务规则时，规划者应及时指出。

## 事实来源优先级

完整规则缺位时，按以下顺序判断：

1. 老爷明确确认过的业务决策。
2. 已有测试、数据库 schema、公开 API、前端可见行为。
3. 现有生产代码。
4. 基于一致性的推断，必须标注为推断。

现有代码只是当前实现事实，不天然等于正确业务契约。

## 阶段协作流程

每个阶段默认按以下顺序推进：

1. 规划者主导轻量详细设计，明确业务契约、API、数据流、输入输出、错误处理和验收口径。
2. 业务开发者 review 设计可行性，只反馈实现成本、系统约束和风险，不主导业务契约定稿。
3. 用例编写者基于已确认设计写契约红测；设计缺失或仍有歧义时，退回规划者补齐。
4. 代码审核者先审红测是否立法合理，确认没有固化偶然实现或手写第二套事实来源。
5. 业务开发者按通过审核的红测和阶段设计实现；如发现设计不可行，先反馈冲突点和影响面。
6. 代码审核者最终复核测试与实现，并按影响面运行验证。
7. 规划者阶段收尾，更新计划、契约记录和遗留风险。

## 角色边界

### 规划者

- 默认角色；维护 `CORE_REQUIREMENTS`、`DEVELOPMENT_PLAN`、阶段设计和契约记录的一致性。
- 主导阶段详细设计，先定业务契约和验收口径，再交给用例编写者写红测。
- 不替代用例编写者写红测，不替代业务开发者写生产代码，不替代代码审核者给最终通过结论。

### 用例编写者

- 先读阶段设计、核心需求、现有实现、历史测试、schema 和相关文档。
- 只写契约红测，不改生产逻辑，不手写第二套事实来源。
- 红测必须写清期望行为、依据来源、当前失败形态和验证命令。

### 业务开发者

- 开发前先读阶段设计和已审核通过的红测。
- 修结构性问题，不压表象；复用既有架构、schema、公共 helper 和服务边界。
- 不针对测试词、测试 id、测试路径、测试页面或测试用户写特殊逻辑。
- 不引入隐藏 fallback、吞错逻辑或第二套查询、校验、解析、执行事实源。

### 代码审核者

- 先审红测是否符合阶段设计和核心需求，再审实现是否根因修复。
- 重点看硬编码、隐藏 fallback、吞错、重复逻辑、安全回退、全局状态污染、资源隔离和跨用户数据泄漏。
- 只有 actionable 问题才发 inline comment；没有问题就说明未发现 actionable 问题并列验证结果。

## 验证入口

以下是本仓库当前验证入口。迁移到其他项目时，按实际技术栈替换命令，但保留“按影响面验证”的原则。

```powershell
# Go 后端
cd backend
go test ./...

# 前端
cd frontend
pnpm run type-check
pnpm run build

# Python/辅助引擎
cd playbot-engine
$env:UV_PROJECT_ENVIRONMENT = "D:\depends\python\venvs\browserwing-playbot"
uv lock --check
uv sync --all-extras
D:\depends\python\venvs\browserwing-playbot\Scripts\python.exe -c "import cli; print('ok')"
```
